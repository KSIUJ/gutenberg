services:
  db:
    image: postgres:15
    environment:
      POSTGRES_DB: gutenbergdb
      POSTGRES_USER: gutenberguser
      POSTGRES_PASSWORD: gutenbergpass
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7
    container_name: gutenberg-redis

  backend:
    build:
      context: .
      dockerfile: Dockerfile
      target: run_backend
    container_name: gutenberg-backend
    environment:
      # TODO: Remove this env variable or copy it to the Celery container too and verify how it interacts with the
      #       settings in docker_settings.py. The REDIS_URL should be configurable, because some users might want to
      #       use multiple Celery instances or put them on different machines.
      - REDIS_URL=redis://redis:6379
    volumes:
      - backend_logs:/var/log/gutenberg
      - type: bind
        source: ./backend/gutenberg/settings/docker_settings.py
        target: /etc/gutenberg/docker_settings.py
    depends_on:
      - db
      - redis

  celery:
    build:
      context: .
      dockerfile: Dockerfile
      target: run_celery
    depends_on:
      - redis
      - db
    volumes:
      - backend_logs:/var/log/gutenberg
      - type: bind
        source: ./backend/gutenberg/settings/docker_settings.py
        target: /etc/gutenberg/docker_settings.py

  proxy:
    build:
      context: .
      dockerfile: Dockerfile
      target: run_nginx
    container_name: gutenberg-proxy
    ports:
      - "3000:80"
    depends_on:
      - backend

volumes:
  postgres_data:
  db_data:
  backend_logs:
